# CTR-OBSERVABILITY — Debug, test và recovery

> **DRI:** QA Lead + Tools Engineer
> **Status:** `DISCOVERY`

Mỗi system quan trọng cung cấp state summary, recent event trace, correlation IDs, rejection/failure reason, relevant seed/version và safe recovery command. Debug tools không được thay đổi save âm thầm; mutation có audit.

Gate/quest/reward/action/Arise cần idempotency ledger. AI cần decision trace. Content cần validation report. Performance cần label/budget attribution. Save cần migration report và invariant audit. Những hook này là Definition of Ready, không là ticket “debug sau”.

Mỗi playable snapshot phải có một diagnostics export gom build/schema/config/mod list, world/player context, recent trace, error/performance sample và screenshot marker phù hợp. User không phải tự tìm log. Debug mutation có permission, confirmation và audit; export mặc định loại đường dẫn/token/chat hoặc dữ liệu riêng tư.
